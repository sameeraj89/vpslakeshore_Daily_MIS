# VPS Lakeshore — Daily Revenue Dashboard

Self-contained dashboard for LHRC daily revenue MIS (`index.html`).
Rebuilt from `Daily Revenue Flash_New_*.xlsx` + `Daily MIS *.xlsx` drops via
`tools/build_dashboard.py`. See `MIS_Report_Prompt.md` for the source-data →
report prompt.
